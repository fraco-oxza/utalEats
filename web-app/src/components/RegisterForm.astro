<form action="" id="registerFormPart1">
    <label for="email">Email</label><br>
    <input type="text" id="email" name="email" placeholder="Email"><br><br>
    <label for="password">Password</label><br>
    <input type="password" id="password" name="password" placeholder="Password"><br><br>
    <label>Full Name</label><br>
    <input type="text" class="name" id="first-name" name="first-name" placeholder="First name">
    <input type="text" class="name" id="last-name" name="last-name" placeholder="Last name"><br><br>
    <p id="error"></p>
    <br><button type="submit">Continue</button><br><br>
    <a href="login">I have an account</a>
</form>
<style>
    form {
        background-color: var(--base-clr);
        box-shadow: 0px 4px 4.7px 5px rgba(0, 0, 0, 0.09);
        border-radius: 10%;
        padding: 30px;
        width: fit-content;
    }

    .name {
        background-color: #D9D9D9 !important;
        border: 1px solid var(--base-dkr-clr);
        border-radius: 6px;
        padding-left: 10px;
    }

    input[type=text] {
        background-color: var(--base-dk-clr);
        border: 1px solid var(--base-dkr-clr);
        border-radius: 6px;
        padding-left: 10px;
    }

    input[type=password] {
        background-color: var(--base-dk-clr);
        border: 1px solid var(--base-dkr-clr);
        border-radius: 6px;
        padding-left: 10px;
    }

    button {
        background-color: var(--primary-clr);
        border: none;
        border-radius: 20px;
        padding: 8px 30px;
        color: var(--primary-fnt-clr);
        font-size: 14px;
        margin: 0 40px;
    }

    a {
        display: block;
        text-align: center;
        color: black;
        font-size: 12px;
    }

    p {
        font-size: 12px;
        color: var(--primary-clr);
    }
</style>